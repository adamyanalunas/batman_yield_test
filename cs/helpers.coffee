# YieldDemo.YieldHelper = (source, container, context=new Batman.View) ->
#   config = into: container
#   config[if source.indexOf('<') isnt -1 then 'html' else 'source'] = source

#   context.render config

